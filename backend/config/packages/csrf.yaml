# Enable stateless CSRF protection for forms and logins/logouts
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
            # 'authenticate' doit rester stateful en dev pour permettre un login depuis React via CSRF.
            # - authenticate
            - logout
