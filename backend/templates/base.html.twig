<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}EduLearn{% endblock %}</title>

    <!-- Bootstrap CSS -->
    <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
    >

    <link rel="stylesheet" href="/styles/app.css">
</head>
<body>

<nav class="navbar edl-navbar">
    <div class="container">
        <a class="navbar-brand edl-brand" href="{{ path('home') }}">
            EduLearn
        </a>

        <div class="d-flex align-items-center gap-2">
            {% if app.user %}
                <a class="btn btn-outline-secondary edl-btn-pill" href="{{ path('app_logout') }}">
                    Déconnexion
                </a>
            {% else %}
                <button class="btn btn-primary btn-sm" data-bs-toggle="modal" data-bs-target="#loginModal">
                    🔐 Connexion
                </button>
            {% endif %}
        </div>
    </div>
</nav>

<main class="py-4">
    <div class="container edl-main-container">
        {% block body %}{% endblock %}
    </div>
</main>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
